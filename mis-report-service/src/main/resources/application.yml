spring:
  application:
    name: mis-report-service
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/hms_userdb
    username: root
    password: Santosh@416
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://host.docker.internal:8761/eureka
  instance:
    prefer-ip-address: true

server:
  port: 8018


